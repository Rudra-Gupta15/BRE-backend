"""Shared-page HTTP routers: auth, system/reset, dashboard, ML-security,
the data-source catalogue + per-source rule toggles, AI/LLM config, and the
external (bank/company) database connection probe."""
